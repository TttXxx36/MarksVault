# MarksVault

MarksVault 管理浏览器书签，并通过可配置任务执行备份、恢复、整理和推送操作。

## Language

**任务**：
一项持久化的自动化配置，定义触发条件和需要执行的书签操作。

**启用状态**：
表示任务是否允许被事件自动触发的稳定配置。用户可以明确手动执行已禁用任务，且
执行成功或失败不会自行改变启用状态。
_Avoid_：任务状态

**任务执行**：
任务被用户或事件触发后形成的一次完整运行，最终只产生一个执行结果。
_Avoid_：运行实例

**执行请求**：
用户或事件提出运行任务的请求。只有初始执行记录和所需租约可靠保存后，执行请求
才被接受并形成任务执行；被拒绝的请求不进入执行历史。
_Avoid_：任务执行

**执行尝试**：
任务执行内部的一次操作尝试；首次尝试和后续重试都属于同一次任务执行。
_Avoid_：任务执行、重试任务

**执行租约**：
一项任务在限定时间内已有任务执行负责运行的声明，用于阻止同一任务被重复执行。
租约到期表示原任务执行已失去运行所有权。
_Avoid_：执行锁、任务锁

**书签写入租约**：
一次任务执行在限定时间内独占修改本地书签树的声明。它允许只读或上传操作并行，
但阻止多个任务同时修改书签树。
_Avoid_：全局任务锁、书签锁

**执行输入**：
仅对一次任务执行有效的临时选择，例如恢复文件或选中的书签。执行输入不会修改任务
本身的触发条件、启用状态或持久化操作配置。
_Avoid_：运行时任务、临时任务

**执行结果**：
一次任务执行的最终记录，分别说明书签操作是否完成，以及该结果是否被可靠保存。
操作成功但记录失败不等同于操作失败。
_Avoid_：操作结果

**执行中断**：
任务执行因扩展后台终止而停止，系统无法继续原执行尝试。执行中断不会自动续跑。
_Avoid_：执行失败、执行超时

**结果不确定**：
系统无法确认任务操作是否完成的执行结果，通常发生在超时或执行中断后仍可能存在
外部副作用时。结果不确定不得自动重试。
_Avoid_：失败、已取消

**执行来源**：
说明一次任务执行由用户明确发起还是由事件自动触发。执行来源属于本次执行，不由
任务配置的触发条件推断。
_Avoid_：触发器类型

**任务快照**：
任务执行开始时固定下来的任务定义。任务后续被编辑或删除，不会改变已开始的任务
执行及其执行尝试。
_Avoid_：任务副本、运行时任务

## AI 分类与恢复

**AI 分类计划**：
AI 根据书签信息生成、供用户审核的分类变更方案。它描述预期的分类和移动，不等同于已经写入书签树的结果。
_Avoid_：AI 结果、分类运行

**AI 分类回滚**：
撤销最近一次已经执行的 AI 分类变更，使受影响的书签尽量回到执行前的位置。
_Avoid_：历史恢复、完整恢复

**书签快照**：
某个时间点浏览器书签树的可恢复记录，包含用户为该记录指定的名称和时间信息。
_Avoid_：任务快照、书签备份点

**书签快照恢复**：
用户从历史书签快照选择性地恢复书签排列和分类的操作；恢复前必须能够查看当前状态与快照之间的差异。
_Avoid_：回滚、强制覆盖

**补充提示词**：
用户对 AI 分类偏好的额外说明，必须与系统规定的输出格式和安全约束共同生效。
_Avoid_：完整提示词覆盖、自由提示词

**AI 配置草稿**：
用户尚未正式提交但希望在设置界面关闭或重新打开后仍能继续编辑的 AI 配置内容。
_Avoid_：正式配置、临时任务

**AI 分类批次**：
一次 AI 分类任务中被同时发送给服务处理的一组书签。批次独立记录状态，某个批次失败不代表其他已完成批次需要重新执行。
_Avoid_：分类分片、单次分类任务

**JSON 修复请求**：
在 AI 返回无法解析的内容后发起的一次限定性重试，只要求服务返回唯一、完整且无额外文本的分类 JSON。
_Avoid_：重新分类、无限重试

**失败批次**：
AI 分类批次因网络、服务响应或 JSON 校验失败而尚未形成可靠结果的批次。失败批次可以单独重试，已完成批次不应重复请求。
_Avoid_：失败任务、未分类任务

**AI 分类任务**：
用户明确启动、跨多个 AI 分类批次生成一个完整分类预览的工作。它独立于弹出窗口的生命周期，并在用户重新打开插件时显示当前状态。
_Avoid_：分类窗口、一次请求

**AI 分类任务草稿**：
尚未完成的 AI 分类任务的可恢复记录，包含已完成批次、失败批次和下一步所需的任务状态。
_Avoid_：AI 配置草稿、半成品分类

**AI 分类预览草稿**：
AI 分类任务完成后、用户确认写入书签前保存的只读分类方案。它可以被重新打开和审核，但不会自动修改书签树。
_Avoid_：分类结果、自动分类

**快照索引**：
书签快照的轻量目录，记录名称、时间、来源、大小、节点数和校验状态，并指向本地保存的完整快照内容。
_Avoid_：完整快照、书签树副本

**自动快照**：
由受保护操作在执行前自动创建的书签快照。自动快照受最近数量保留策略管理，但不能因此删除用户保护的命名快照。
_Avoid_：临时备份、无条件备份

**命名快照**：
用户主动创建并指定名称的书签快照。命名快照默认受保护，只有用户明确删除或取消保护后才可清理。
_Avoid_：自动快照、快照草稿

**快照差异**：
对比某个书签快照与当前书签树后得到的新增、删除、移动、重命名、冲突和跳过项目清单。
_Avoid_：恢复结果、覆盖清单

**快照冲突**：
快照节点无法通过稳定书签 ID，或通过受限的标题、URL 与文件夹路径指纹唯一匹配当前节点的情况。冲突默认跳过，不得仅凭标题猜测。
_Avoid_：模糊匹配、自动合并

**恢复计划**：
用户确认前由快照差异生成的选择性恢复方案，明确每个节点的恢复、跳过或冲突状态；恢复计划本身不执行书签写入。
_Avoid_：强制恢复、恢复结果

**恢复日志**：
恢复计划对每个实际书签操作的持久化逐项记录，包含操作状态、时间、错误和可回滚所需的前后位置。
_Avoid_：运行日志、恢复草稿

**选择性恢复**：
仅执行恢复计划中经用户确认且仍满足安全条件的节点操作，不删除快照之后新增的书签、不修改 URL，也不删除非空文件夹。
_Avoid_：全量覆盖、强制同步

**GitHub 备份 schema v2**：
MarksVault 书签备份的语义根目录格式，保留根目录角色、节点类型、路径指纹和校验元数据；新备份使用该格式，旧 v1 文件只作为兼容输入。
_Avoid_：GitHub 文件快照、无版本备份

**备份迁移**：
将旧版 GitHub 书签备份转换为内部的 schema v2 和本地导入快照；迁移过程必须先完成结构校验，不能绕过差异预览直接写入书签。
_Avoid_：静默升级、直接覆盖

**导入保底文件夹**：
当快照中的语义根目录无法映射到当前浏览器时，为本次恢复创建的明确命名文件夹，用于保留未知根目录内容；文件夹 ID 由恢复日志记录以保证继续执行幂等。
_Avoid_：丢弃未知根目录、强行映射书签栏

**GitHub 结构化错误**：
带有错误类别、HTTP 状态、限流元数据和安全重试建议的 GitHub 操作结果。认证、权限、校验和冲突错误不得套用网络重试。
_Avoid_：原始异常文本、所有错误都重试

**AI 请求预算**：
一次 AI 分类请求和一个分类批次允许消耗的最长时间与尝试次数。请求预算和批次预算分别约束单次网络等待与一组书签的总等待，预算耗尽后该批次进入可恢复失败。
_Avoid_：无限等待、只按弹窗生命周期计时

**AI 分类进度心跳**：
用户界面定期读取后台 AI 分类任务的最新状态，用于展示批次、尝试次数、耗时和失败原因。它不拥有任务，也不因弹窗关闭而取消任务。
_Avoid_：把 Popup 是否打开当作任务状态

**自适应分类批次**：
当一批书签因响应超时、批次预算耗尽或请求内容过大而无法完成时，在同一批次预算内拆成更小的子批次；拆分不得绕过取消、恢复和检查点规则。
_Avoid_：无限拆分、重新发送已完成子批次
