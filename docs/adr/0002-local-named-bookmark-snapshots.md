# 使用本地命名书签快照进行历史恢复

状态：accepted。每次 AI 分类执行前创建本地命名书签快照，并保留最近的自动快照和用户保护的命名快照；历史恢复先生成差异预览，默认跳过快照之后发生变化的节点。快照不自动上传 GitHub，因为它可能包含完整书签 URL，用户可以显式导出或导入。

考虑过的方案：只保留最近一次撤销、自动把快照推送到 GitHub、无条件覆盖当前书签树。最终选择本地历史加冲突安全恢复，避免隐私扩散和误删用户后续修改，同时保留恢复能力。

