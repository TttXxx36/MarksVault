# 使用租约和恢复日志执行冲突安全的选择性恢复

状态：accepted。书签快照恢复必须先生成差异和恢复计划，用户确认后取得书签写入租约，并在每个节点写入后立即保存恢复日志；中断或写入异常标记为“结果不确定”，浏览器重启只恢复可见状态而不自动继续外部写操作。匹配优先使用稳定 ID，只有唯一的标题、URL 和文件夹路径指纹才允许回退匹配。

考虑过的方案：直接覆盖整棵书签树、仅依赖一次性撤销或在后台自动重试。它们会误删用户后来新增的书签、吞掉并发修改，或在结果不确定时重复产生副作用，因此采用可审计、可继续/回滚且默认跳过冲突的恢复模型。
